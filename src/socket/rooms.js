export const adminRoom = () => "admin";

export const customerRoom = (userId) =>
    `customer_${userId}`;

export const partnerRoom = (partnerId) =>
    `partner_${partnerId}`;

export const orderRoom = (orderId) =>
    `order_${orderId}`;